subprojects {
    task sleep(type: SleepExec)
}

class SleepExec extends DefaultTask {
    @TaskAction
    void sleep() {
        project.exec {
            commandLine 'sleep', 1
            workingDir project.rootDir
        }
    }
}
